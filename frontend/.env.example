# api is the default. fixture must be explicitly selected for a design preview.
VITE_DATA_SOURCE=api
VITE_STRATEGY_ID=demo-project-planning
# Public same-origin prefix only; never place model credentials in frontend variables.
VITE_API_BASE=
